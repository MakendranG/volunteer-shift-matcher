# Core Strands Agents SDK (provides `from strands import Agent, tool`)
strands-agents>=1.0.0
# Community tools package (optional but recommended alongside the SDK)
strands-agents-tools>=0.2.0
# Loads config from a local .env file (keeps API keys out of the code)
python-dotenv>=1.0.0
# Visual web UI (streamlit run streamlit_app.py)
streamlit>=1.36.0
# Test runner
pytest>=8.0.0
